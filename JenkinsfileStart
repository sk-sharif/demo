pipeline {
  agent any
  stages {
     stage('Start the service') {
      steps{
        withAWS(credentials: 'AWS-Jenkins-Cred', region: 'ap-south-1') {
          sh 'chmod 777 start.sh'
          sh './start.sh'
        }
      }
    }
  }
}
